#!/bin/bash
# Создание мастер-списка URL с умной дедупликацией

DOMAIN="$1"
MIN_SIZE="${2:-5000}"

if [ -z "$DOMAIN" ]; then
    echo "Использование: $0 DOMAIN [MIN_SIZE]"
    echo "Пример: $0 sluga-narodu.com 5000"
    exit 1
fi

echo "🚀 Создание мастер-списка URL для $DOMAIN (с дедупликацией)"
echo "============================================================"

# Шаг 1: Получение всех HTML URL
echo "🔍 Шаг 1: Получение всех HTML URL..."
curl -s "http://web.archive.org/cdx/search/cdx?url=${DOMAIN}/*&output=json&fl=original,timestamp,statuscode,mimetype,length&filter=mimetype:text/html&filter=statuscode:200&collapse=original&limit=50000" > ${DOMAIN}_raw.json

raw_count=$(($(jq length ${DOMAIN}_raw.json) - 1))
echo "✅ Найдено уникальных HTML URL: $raw_count"

# Шаг 2: Базовая фильтрация + исключение технических вариантов
echo ""
echo "🧹 Шаг 2: Фильтрация технических страниц и вариантов..."

jq -r --argjson min_size "$MIN_SIZE" '
if length > 1 then
  .[1:] | map({
    original: .[0],
    timestamp: .[1],
    statuscode: .[2],
    mimetype: .[3],
    size: (.[4] | tonumber? // 0)
  }) | map(select(
    # Размер страницы
    (.size > $min_size) and

    # Основные технические фильтры
    (.original | test("/wp-admin|/admin|/login|/auth|/dashboard") | not) and
    (.original | test("/api/|/rest/|/json/|/xml/|/ajax") | not) and
    (.original | test("/css/|/js/|/images/|/img/|/assets/|/static/|/media/") | not) and
    (.original | test("/404|/error|/maintenance|/503") | not) and
    (.original | test("robots\\.txt|sitemap\\.xml|\\.well-known/") | not) and
    (.original | test("/search|/tag/|/category/|/archive") | not) and
    (.original | test("\\?page=|\\?p=|/page/|\\?offset=") | not) and
    (.original | test("/feed|/rss|/atom|/trackback|/xmlrpc") | not) and

    # Технические варианты контента
    (.original | test("/embed/?$|\\?amp=1|\\?utm_|/amp/?$|/print/?$|\\?preview=") | not) and

    # Расширения файлов
    (.original | test("\\.(css|js|png|jpg|jpeg|gif|ico|pdf|zip|doc|docx|txt|xml)$") | not) and

    # CMS специфичные пути
    (.original | test("/wp-content|/wp-includes|/wp-json") | not) and
    (.original | test("/node_modules|/vendor|/.git") | not) and

    # Служебные страницы
    (.original | test("/debug|/test|/tmp|/temp") | not)
  ))
else
  []
end
' ${DOMAIN}_raw.json > ${DOMAIN}_filtered.json

filtered_count=$(jq length ${DOMAIN}_filtered.json)

# Шаг 3: Умная дедупликация похожих URL
echo ""
echo "🔄 Шаг 3: Дедупликация похожих URL..."

jq -r '
# Создаем нормализованный ключ для группировки
map(. + {
  normalized_key: (.original |
    # Убираем trailing slash для единообразия
    gsub("/$"; "") |
    # Убираем номерные суффиксы (-2, -3, etc)
    gsub("-[0-9]+$"; "") |
    # Убираем query параметры
    split("?")[0]
  )
}) |
# Группируем по нормализованному ключу
group_by(.normalized_key) |
map(
  # Из каждой группы выбираем лучший вариант
  sort_by([
    # Приоритет: НЕ имеет номерной суффикс
    (.original | test("-[2-9]") | not),
    # Приоритет: больший размер
    -.size,
    # Приоритет: более свежий timestamp
    -.timestamp
  ]) | .[0]
) |
# Убираем временный ключ
map(del(.normalized_key))
' ${DOMAIN}_filtered.json > ${DOMAIN}_deduped.json

deduped_count=$(jq length ${DOMAIN}_deduped.json)
duplicates_removed=$((filtered_count - deduped_count))

# Шаг 4: Финальный мастер-список
echo ""
echo "📊 Шаг 4: Создание финального мастер-списка..."

jq '[.[] | {
  original: .original,
  first_archived: .timestamp,
  size: .size,
  priority_score: (
    (.size / 1000) +
    (50 - (((.original | split("/") | length) - 3) * 5))
  )
}] | sort_by(-.priority_score)' ${DOMAIN}_deduped.json > ${DOMAIN}_master_list.json

# Результаты
echo ""
echo "✅ МАСТЕР-СПИСОК СОЗДАН!"
echo "========================"
echo "📊 Статистика:"
echo "  Всего найдено URL: $raw_count"
echo "  После базовой фильтрации: $filtered_count"
echo "  Удалено дубликатов: $duplicates_removed"
echo "  Финальный список: $deduped_count"
echo ""

# Показать примеры удаленных дубликатов
echo "🔍 Примеры обработки дубликатов:"
echo "Исходные варианты zbory-zakarpatskoi... → Выбран лучший вариант"

echo ""
echo "🎯 ТОП-10 URL по приоритету:"
jq -r '.[] | "\(.priority_score | floor)\t\(.original)"' ${DOMAIN}_master_list.json | head -10 | while IFS=$'\t' read score url; do
    echo "  $score: $url"
done

echo ""
echo "📁 Файлы:"
echo "  ${DOMAIN}_master_list.json - финальный мастер-список (ИСПОЛЬЗУЙТЕ ЭТОТ)"